# falais-proxy

Gets free proxy credentials from Urban VPN is unofficial API.

# How is working?

1. Registers an anonymous user (impersonate browser extension).
2. Gets an access token saved to `access_token.json`
3. Getting list available countries.

## Usage

```
pip install -r requirements.txt
python main.py
```
